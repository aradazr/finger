# ✋😃 شمارش انگشت + تشخیص حالت چهره با MediaPipe

با وب‌کم انگشت‌ها را می‌شمارد، ژست‌های OK/Peace/Like/Dislike را می‌فهمد و با FaceMesh حدس می‌زند خوشحالید یا ناراحت یا عادی. خروجی روی تصویر است و اگر TTS نصب باشد با شما حرف می‌زند.

## ⚡ پیش‌نیازها
- Python 3.8+
- وب‌کم
- پکیج‌ها: `opencv-python`, `mediapipe`, (اختیاری) `pyttsx3`

نصب سریع:
```bash
pip install opencv-python mediapipe pyttsx3
```
اگر صدا نمی‌خواهید، `pyttsx3` لازم نیست.

## ▶️ اجرای سریع
```bash
cd /Users/aradazr/Desktop/finger
python finger_counter.py
```
خروج: کلید `q`

## 🎯 امکانات
- ✋ شمارش انگشت برای هر دست + نمایش مجموع.
- 🤟 ژست‌ها: OK، Peace/V، Like (شست بالا)، Dislike (شست پایین).
- 🙂😐☹️ حدس Emotion (Happy / Sad / Neutral) و نمایش ایموجی.
- 💡 هشدار نور کم.
- 🧾 ثبت رویدادها در `EVENT_BUFFER` (آماده برای API/وب‌سوکت).
- 🔊 بازخورد صوتی اگر `pyttsx3` نصب باشد.

## 🧠 ساختار و نکات کد
- فایل اصلی: `finger_counter.py`
- MediaPipe Hands برای دست، FaceMesh برای چهره.
- توابع مهم:
  - `count_fingers(...)`: شمارش انگشت‌ها.
  - `classify_gesture(...)`: تشخیص ژست‌های ساده.
  - `detect_emotion(...)`: حدس حالت چهره با هیوریستیک دهان/لب.
  - `speak(...)`: گفتار متن در صورت وجود `pyttsx3`.
  - `log_event(...)`: ذخیره رویداد در بافر کوچک.
- آستانه‌ها (Emotion/نور/ژست) ساده هستند و به‌راحتی قابل تنظیم‌اند.

## 🛠️ نکات اجرا و عیب‌یابی
- وب‌کم باز نشد؟ `cv2.VideoCapture(0)` را به 1 یا 2 تغییر دهید اگر چند دوربین دارید.
- صدا کار نکرد؟ `pyttsx3` را نصب کنید و مجوز اسپیکر را در مک تأیید کنید.
- نور کم = دقت پایین؛ نور محیط را بهتر کنید.

## 🚀 تمرین و توسعه بیشتر
- افزودن سویچ CLI برای خاموش/روشن کردن TTS یا Emotion.
- ارسال رویدادهای `EVENT_BUFFER` به سرور یا داشبورد وب با وب‌سوکت.
- بهبود هیوریستیک Emotion یا استفاده از یک مدل سبک صورت.
- ژست‌های بیشتر و اکشن برای هر ژست (مثلاً اسکرین‌شات یا پخش افکت صوتی).

